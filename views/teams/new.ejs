<%- include('../partials/header') %>

<body>
  <h1> <%= title %></h1>
<br>
<p><%=teams.data[22].players[0].first_name%></p>
<br>
<br>

<form id="add-team-nav" method="GET" action="/teams/<%= user._id %>/new">
<label>Team Name:</label>
    <input name="name">
    <br>


 <label>Point Guard:</label>
  <div class="dropdown show">
    <a class="btn btn-primary dropdown-toggle" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
     Select a Team
    </a>
  
    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <% teams.data.forEach(function(t) { %>
            <a class="dropdown-item" href="#"> <%=t.full_name %> </a>
            <% }); %>
      
    </div>
  </div>

 <label>Player:</label>
  <div class="dropdown show">
    <a class="btn btn-primary dropdown-toggle" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
     Select a Team
    </a>
  
    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <% teams.data[22].players.forEach(function(p) { %>
            <a class="dropdown-item" href="#"> <%=p.first_name %> <%=p.last_name %> </a>
            <% }); %>
      
    </div>
  </div>




 <!-- <label>Point Guard:</label>
 <label>Team:</label>
            <select name="team" onchange="teamToPlayerPG">
                <% teams.data.forEach(function(t) { %>
                <option value="<% t.id %>"> <%=t.full_name %></option>
                <% }); %>
            </select> <br> -->

<div>
 
<br>



<button type="button" class="btn btn-secondary">Finalize Team</button>
</form>
     


<%- include('../partials/footer') %>